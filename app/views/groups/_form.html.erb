<%= form_for(@group, :url => user_groups_path(current_user), :method => 'post') do |f| %>
<div><%= f.label :name, "Name"  %><br />
<%= f.text_field :name, :autofocus => true %></div>
<div><%= f.submit "Create group" %></div>
<% end %>